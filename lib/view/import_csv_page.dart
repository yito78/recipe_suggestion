import 'package:flutter/material.dart';

class ImportCsvPage extends StatelessWidget {
  const ImportCsvPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("CSV取り込み画面");
  }
}
